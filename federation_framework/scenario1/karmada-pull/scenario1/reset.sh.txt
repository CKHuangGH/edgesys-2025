#!/bin/bash

# 查找所有 tcpdump 相關的進程並刪除
echo "查找並終止 tcpdump 相關進程..."

# 使用 pgrep 找到所有 tcpdump 和 sudo tcpdump 進程的 PID
PIDS=$(pgrep -f "tcpdump")

# 如果找到進程，則終止它們
if [ -n "$PIDS" ]; then
    echo "找到以下進程: $PIDS"
    kill -9 $PIDS
    echo "所有 tcpdump 進程已終止。"
else
    echo "未找到 tcpdump 進程。"
fi
